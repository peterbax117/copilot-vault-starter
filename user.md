---
budget_words: 1024
hard_cap_words: 1024
role: user
last_audit: TEMPLATE
---

# User Profile — <YOUR NAME>

**Purpose:** WHO you are and HOW you want the agent to work, on top of the
durable persona in `core/identity-core.md`. Loaded every session, so keep it
tight. Push depth into `archive/<topic>.md` and reference it from `memory.md`.

> Fill in the sections below. Delete anything that does not apply. Keep the total
> under the word cap in the frontmatter (the health check enforces it).

## Identity   `<<< FILL IN >>>`

- **Role / team:**
- **Accounts & identities** the agent should keep straight (e.g. work vs personal
  GitHub, which to use where). List any hard rules — mixing these up is a common
  agent mistake.
- **Hardware / environment quirks** worth knowing.

## How I want the agent to communicate   `<<< FILL IN >>>`

- Add any voice/tone rules specific to your work that go beyond
  `core/identity-core.md` (e.g. email tone, sign-off, audiences).

## Domain terminology   `<<< FILL IN >>>`

- Internal tools, acronyms, or names the agent must use correctly, and any
  "do not confuse X with Y" rules.

## File-output preferences   `<<< FILL IN >>>`

- **Default destination** for user-facing deliverables (a folder you actually
  browse). Restate the "not session-state" rule here if it matters to you.
- Any format defaults (HTML vs Markdown vs DOCX) and post-processing steps.
